name: RDP
on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Configurar RDP
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall delete rule name="RDP-Tailscale" 2>$null
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Criar Usuário
        run: |
          Add-Type -AssemblyName System.Security
          $charSet = @{
              Upper   = [char[]](65..90)
              Lower   = [char[]](97..122)
              Number  = [char[]](48..57)
              Special = ([char[]](33..47) + [char[]](58..64) + [char[]](91..96) + [char[]](123..126))
          }
          $rawPassword = @()
          $rawPassword += $charSet.Upper | Get-Random -Count 4
          $rawPassword += $charSet.Lower | Get-Random -Count 4
          $rawPassword += $charSet.Number | Get-Random -Count 4
          $rawPassword += $charSet.Special | Get-Random -Count 4
          $password = -join ($rawPassword | Sort-Object { Get-Random })
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "Skyro" -Password $securePass -AccountNeverExpires -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Administrators" -Member "Skyro" -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "Skyro" -ErrorAction SilentlyContinue
          echo "RDP_CREDS=Usuário: Skyro | Senha: $password" >> $env:GITHUB_ENV

      - name: Instalar Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath -UseBasicParsing
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Conectar Tailscale
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          if (-not $tsIP) { Write-Error "Falha ao obter IP"; exit 1 }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Preparar Roblox — SEM ERRO DE CERTIFICADO!
        run: |
          # Desliga aceleração de vídeo para funcionar em VM
          Set-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\DirectX" -Name "DisableHWAcceleration" -Value 1 -Force

          # ⚠️ DESATIVA VERIFICAÇÃO DE CERTIFICADO — resolve o erro de nome inválido
          Add-Type @"
          using System.Net;
          using System.Security.Cryptography.X509Certificates;
          public class TrustAllCertsPolicy : ICertificatePolicy {
              public bool CheckValidationResult(
                  ServicePoint srvPoint, X509Certificate certificate,
                  WebRequest request, int certificateProblem) {
                  return true;
              }
          }
"@
          [System.Net.ServicePointManager]::CertificatePolicy = New-Object TrustAllCertsPolicy
          [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.SecurityProtocolType]::Tls12

          # Usa HTTP — certificado não bloqueia mais!
          $dest = "C:\Users\Skyro\Downloads\RobloxPlayerLauncher.exe"
          $url = "http://setup.roblox.com/RobloxPlayerLauncher.exe"
          
          $ok = $false
          for ($i=1; $i -le 5; $i++) {
              try {
                  Write-Host "Tentativa $i de baixar..."
                  Invoke-WebRequest -Uri $url -OutFile $dest -UseBasicParsing -TimeoutSec 60
                  if (Test-Path $dest) { $ok = $true; break }
              } catch {
                  Write-Host "Falha: $_ — tentando novamente em 5s..."
                  Start-Sleep -Seconds 5
              }
          }
          if (-not $ok) { Write-Error "Não consegui baixar o Roblox"; exit 1 }
          Write-Host "✅ Roblox baixado com sucesso!"

          # Configura vídeo para funcionar em VM
          $clientSettingsPath = "$env:LOCALAPPDATA\Roblox\Versions\ClientSettings"
          New-Item -Path $clientSettingsPath -ItemType Directory -Force -ErrorAction SilentlyContinue
          $configContent = '{"FFlagDebugGraphicsPreferD3D11":false,"FFlagDebugGraphicsDisableD3D11":true,"FFlagDebugGraphicsPreferD3D9":true,"FFlagDebugGraphicsDisableOpenGL":true,"DFIntTaskSchedulerTargetFps":60}'
          $configContent | Out-File -FilePath "$clientSettingsPath\ClientSettings.json" -Encoding UTF8 -Force
          Write-Host "✅ Configuração de vídeo pronta"

      - name: Verificar Conexão
        run: |
          Write-Host "IP: $env:TAILSCALE_IP"
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) { Write-Error "Porta 3389 fechada"; exit 1 }
          Write-Host "✅ RDP liberado"

      - name: Manter Ativo e Mostrar Dados
        run: |
          Write-Host "======================================"
          Write-Host "🔗 ENDEREÇO: $env:TAILSCALE_IP"
          Write-Host "👤 USUÁRIO: Skyro"
          Write-Host "🔑 SENHA: $($env:RDP_CREDS.Split('|')[1].Trim())"
          Write-Host "======================================"
          Write-Host "✅ Roblox na pasta Downloads!"
          while ($true) {
              Write-Host "[$(Get-Date)] Em funcionamento..."
              Start-Sleep -Seconds 300
          }
          360
